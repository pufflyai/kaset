import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta = {
  title: "opfs-utils/Playground",
};

export default meta;

type Story = StoryObj;

export const Default: Story = {
  render: () => <div>playground</div>,
};
